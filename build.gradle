buildscript {
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.0.2' // 최신 버전으로 업데이트
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.7.20" // Kotlin 플러그인 업데이트
    }
}